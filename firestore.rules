rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 每個使用者只能讀寫自己那一份文件（文件 ID 就是自己的 Google 帳號 uid）
    match /progress/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
