rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 所有人可讀取（供教師 Dashboard 查看），但只能寫入自己的文件
    match /progress/{uid} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
